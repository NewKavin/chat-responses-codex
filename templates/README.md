# Templates

This directory groups reusable example files by purpose.

- `codex/` contains Codex client examples such as the config file and model catalog
- `state/` contains gateway state examples

`.env.example` stays in the repository root because Docker Compose expects it there by default.
